// ────────────────────────────────────────────────────────────────
//  Strategy Spine OS — backend config
//  Paste your Supabase project values below, then reload.
//  Find them in Supabase: Project → Settings → API.
//  (The anon/public key is safe to ship in the browser — Row-Level
//   Security in supabase-schema.sql is what protects each user's data.)
// ────────────────────────────────────────────────────────────────
window.SPINE_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-PUBLIC-KEY"
};
